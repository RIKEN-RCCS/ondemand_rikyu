<%= set_xfce %>

# Start Paraview
<% if !context.global_input_file.to_s.empty? %>
vglrun -d egl paraview "<%= context.global_input_file %>" &
<% else %>
vglrun -d egl paraview &
<% end %>

# Start up xfce desktop (block until user logs out of desktop)
xfce4-session
