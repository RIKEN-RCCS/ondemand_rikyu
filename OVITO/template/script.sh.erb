<%= set_xfce %>

# Start OVITO
<% if !context.global_input_file.to_s.empty? %>
  vglrun -d egl /opt/ovito/bin/ovito "<%= context.global_input_file %>" &
<% else %>
  vglrun -d egl /opt/ovito/bin/ovito &
<% end %>

# Start up xfce desktop (block until user logs out of desktop)
xfce4-session
